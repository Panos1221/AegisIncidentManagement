<Window x:Class="AegisDispatcher.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AegisDispatcher - Login"
        Icon="pack://application:,,,/Resources/Icons/AppIcons/AppIcon_circle.ico"
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Enhanced Button Style with Hover Effects -->
        <Style x:Key="LoginButton" TargetType="Button">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF0099FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF0066CC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Demo Account Button Style -->
        <Style x:Key="DemoButton" TargetType="Button">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                            VerticalAlignment="Center" 
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF3F3F46"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007ACC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF4A4A4A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Enhanced TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FF3C3C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="#FF007ACC"/>
            <Setter Property="BorderBrush" Value="#FF626262"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" 
                                        HorizontalScrollBarVisibility="Hidden" 
                                        VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007ACC"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Enhanced PasswordBox Style -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#FF3C3C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="#FF007ACC"/>
            <Setter Property="BorderBrush" Value="#FF626262"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" 
                                        HorizontalScrollBarVisibility="Hidden" 
                                        VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF007ACC"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close Button Style -->
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFFF4444"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Animations -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                           From="0" To="1" Duration="0:0:0.8"/>
        </Storyboard>
        
        <Storyboard x:Key="SlideInFromLeft">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" 
                           From="-100" To="0" Duration="0:0:0.6"/>
            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                           From="0" To="1" Duration="0:0:0.6"/>
        </Storyboard>
        
        <Storyboard x:Key="SlideInFromRight">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" 
                           From="100" To="0" Duration="0:0:0.6"/>
            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                           From="0" To="1" Duration="0:0:0.6"/>
        </Storyboard>
    </Window.Resources>

    <Border Background="#FF1E1E1E" 
            CornerRadius="12" 
            BorderBrush="#FF3F3F46" 
            BorderThickness="1">
        <!-- Main Content -->
        <Grid>
            <!-- Close Button -->
            <Button x:Name="CloseButton" 
                    Style="{StaticResource CloseButton}"
                    Content="×"
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top" 
                    Margin="15"
                    Panel.ZIndex="10"
                    Click="CloseButton_Click"/>

            <!-- Two Column Layout -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Column - Login Form -->
                <Border Grid.Column="0" Background="#FF1E1E1E" Padding="60">
                    <StackPanel x:Name="LoginFormPanel" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center" 
                               Width="400">
                        <StackPanel.RenderTransform>
                            <TranslateTransform/>
                        </StackPanel.RenderTransform>
                        
                        <!-- Logo and Title -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,40">
                            <Image Source="pack://application:,,,/Resources/Icons/AppIcons/AppIcon_circle_32x32.png" 
                                   Width="56" Height="56" 
                                   Margin="0,0,20,0"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Aegis Dispatcher" 
                                           FontSize="28" 
                                           FontWeight="Bold" 
                                           Foreground="White"/>
                                <TextBlock Text="Incident Management System" 
                                           FontSize="14" 
                                           Foreground="#FFAAAAAA"
                                           Margin="0,5,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Welcome Text -->
                        <TextBlock Text="Welcome back" 
                                   FontSize="20" 
                                   FontWeight="SemiBold" 
                                   Foreground="White" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="Please sign in to your account" 
                                   FontSize="14" 
                                   Foreground="#FFAAAAAA" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,30"/>

                        <!-- Login Form -->
                        <StackPanel>
                            <TextBlock Text="Email Address" 
                                       FontSize="14" 
                                       FontWeight="Medium"
                                       Foreground="White" 
                                       Margin="0,0,0,8"/>
                            <TextBox x:Name="EmailTextBox" 
                                      Style="{StaticResource ModernTextBox}"
                                      KeyDown="InputField_KeyDown"
                                      Margin="0,0,0,20"/>

                             <TextBlock Text="Password" 
                                        FontSize="14" 
                                        FontWeight="Medium"
                                        Foreground="White" 
                                        Margin="0,0,0,8"/>
                             <PasswordBox x:Name="PasswordBox" 
                                          Style="{StaticResource ModernPasswordBox}"
                                          KeyDown="InputField_KeyDown"
                                          Margin="0,0,0,25"/>

                            <Button x:Name="LoginButton" 
                                    Content="Sign In" 
                                    Style="{StaticResource LoginButton}"
                                    Click="LoginButton_Click"
                                    Margin="0,0,0,20"/>

                            <!-- Loading Indicator -->
                            <ProgressBar x:Name="LoadingIndicator" 
                                         IsIndeterminate="True" 
                                         Height="4" 
                                         Visibility="Collapsed"
                                         Margin="0,0,0,15"/>

                            <!-- Error Message -->
                            <TextBlock x:Name="ErrorMessage" 
                                       Foreground="#FFFF6B6B" 
                                       FontSize="12" 
                                       TextWrapping="Wrap" 
                                       Visibility="Collapsed"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,20"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Right Column - Demo Accounts -->
                <Border Grid.Column="1" Background="#FF252526" BorderBrush="#FF3F3F46" BorderThickness="1,0,0,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="DemoAccountsPanel" 
                                   Margin="25,40,25,40">
                            <StackPanel.RenderTransform>
                                <TranslateTransform/>
                            </StackPanel.RenderTransform>
                            
                            <TextBlock Text="Quick Login" 
                                       FontSize="18" 
                                       FontWeight="SemiBold" 
                                       Foreground="White" 
                                       Margin="0,0,0,8"/>
                            <TextBlock Text="Development Accounts" 
                                       FontSize="12" 
                                       Foreground="#FFAAAAAA" 
                                       Margin="0,0,0,25"/>

                            <!-- Demo Dispatcher Accounts -->
                            <Button Style="{StaticResource DemoButton}"
                                    Click="QuickLogin_Click" 
                                    Tag="dispatcher@fireservice.gr" Height="65">
                                <StackPanel>
                                    <TextBlock Text="Fire Service Dispatcher" FontWeight="SemiBold" Foreground="#FFDC2626" FontSize="12"/>
                                    <TextBlock Text="dispatcher@fireservice.gr" Foreground="#FFAAAAAA" FontSize="10" Margin="0,2,0,0"/>
                                    <TextBlock Text="Fire Service" Foreground="#FF888888" FontSize="10" Margin="0,1,0,0"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource DemoButton}"
                                    Click="QuickLogin_Click" 
                                    Tag="dispatcher@coastguard.gr" Height="67">
                                <StackPanel>
                                    <TextBlock Text="Coast Guard Dispatcher" FontWeight="SemiBold" Foreground="#FF0EA5E9" FontSize="12"/>
                                    <TextBlock Text="dispatcher@coastguard.gr" Foreground="#FFAAAAAA" FontSize="10" Margin="0,2,0,0"/>
                                    <TextBlock Text="Coast Guard" Foreground="#FF888888" FontSize="10" Margin="0,1,0,0"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource DemoButton}"
                                    Click="QuickLogin_Click" 
                                    Tag="dispatcher@police.gr" Height="69">
                                <StackPanel>
                                    <TextBlock Text="Police Dispatcher" FontWeight="SemiBold" Foreground="#FF2563EB" FontSize="12"/>
                                    <TextBlock Text="dispatcher@police.gr" Foreground="#FFAAAAAA" FontSize="10" Margin="0,2,0,0"/>
                                    <TextBlock Text="Police" Foreground="#FF888888" FontSize="10" Margin="0,1,0,0"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource DemoButton}"
                                    Click="QuickLogin_Click" 
                                    Tag="dispatcher@ekab.gr" Height="69">
                                <StackPanel>
                                    <TextBlock Text="EKAB Dispatcher" FontWeight="SemiBold" Foreground="#FFCA8A04" FontSize="12"/>
                                    <TextBlock Text="dispatcher@ekab.gr" Foreground="#FFAAAAAA" FontSize="10" Margin="0,2,0,0"/>
                                    <TextBlock Text="EKAB" Foreground="#FF888888" FontSize="10" Margin="0,1,0,0"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Border>

    <!-- Window Load Animation -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
        </EventTrigger>
    </Window.Triggers>
</Window>
