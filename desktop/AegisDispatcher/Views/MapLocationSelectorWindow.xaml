<Window x:Class="AegisDispatcher.Views.MapLocationSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Select Location on Map - AegisDispatcher"
        Height="600" Width="800"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Background="#FF252526">

    <Window.Resources>
        <Style x:Key="MapButton" TargetType="Button">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF1F8AD6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource MapButton}">
            <Setter Property="Background" Value="#FF626262"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF757575"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF4F4F4F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Header -->
        <Border DockPanel.Dock="Top" Background="#FF1E1E1E" BorderBrush="#FF3F3F46" BorderThickness="0,0,0,1">
            <Grid Margin="20,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Select Location on Map" Foreground="White" FontSize="16" FontWeight="Bold"/>
                    <TextBlock Text="Click on the map to select the incident location" Foreground="#FFCCCCCC" FontSize="12" Margin="0,5,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="ConfirmButton" Content="✓ Confirm Location" Style="{StaticResource MapButton}" 
                            Click="Confirm_Click" IsEnabled="False" Margin="0,0,10,0"/>
                    <Button x:Name="CancelButton" Content="✕ Cancel" Style="{StaticResource CancelButton}" 
                            Click="Cancel_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <Border DockPanel.Dock="Bottom" Background="#FF1E1E1E" BorderBrush="#FF3F3F46" BorderThickness="0,1,0,0">
            <Grid Margin="20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusText" Text="Click on the map to select a location" 
                          Foreground="#FFCCCCCC" FontSize="11" VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="CoordinatesPanel" Visibility="Collapsed">
                    <TextBlock Text="Selected: " Foreground="#FFCCCCCC" FontSize="11" VerticalAlignment="Center"/>
                    <TextBlock x:Name="CoordinatesText" Text="" Foreground="White" FontSize="11" 
                              FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Map Container -->
        <Border Background="#FF1E1E1E" BorderBrush="#FF3F3F46" BorderThickness="1" Margin="10">
            <Grid>
                <wv2:WebView2 x:Name="MapWebView" 
                             NavigationCompleted="MapWebView_NavigationCompleted"/>
                
                <!-- Loading Overlay -->
                <Border x:Name="LoadingOverlay" Background="#CC1E1E1E" Visibility="Visible">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="🗺️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                        <TextBlock Text="Loading Map..." Foreground="White" FontSize="16" HorizontalAlignment="Center"/>
                        <TextBlock Text="Please wait while the map loads" Foreground="#FFCCCCCC" 
                                  FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </DockPanel>
</Window>